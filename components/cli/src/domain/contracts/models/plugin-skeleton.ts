export type PluginSkeleton = {
    identifier: string;
    name: string;
    baseline: string;
    description: string;
    git: string;
    subfolder?: string;
    branch?: string;
    demo?: string;
};
