export type Boilerplate = {
    identifier: string;
    name: string;
    baseline: string;
    description: string;
    demo: string;
    blueprint: string;
    git: string;
}

export type Tenant = {
    identifier: string;
}

export type Tip = {
    title: string;
    url: string;
    type: string;
}
